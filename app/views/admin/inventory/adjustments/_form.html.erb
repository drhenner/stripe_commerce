
<% if @variant.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@variant.errors.count, "error") %> prohibited this purchase_order from being saved:</h2>

    <ul>
    <% @variant.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class='clear'></div>
<div class='span-12'>
  <div class='span-5'>
    <%= f.label :qty_to_add %>
  </div>
  <div class='span-6 last'>
    <%= f.number_field :qty_to_add, {:step => 1} %>
  </div>


    <%= hidden_field_tag :refund, 0.00 %>

</div>

<div class='clear'></div>
